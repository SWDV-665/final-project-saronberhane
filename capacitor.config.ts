import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Week-8-FinalProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
